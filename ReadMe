Some of the files are encrypted and not accessible.

The Key File "EHKey.key" is the unique key for decrypting the files
(Important not the lose "EHKey.key")

To successfully unlock the files you needs to have both the Key File and the Master Key 
